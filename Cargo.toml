[package]
name = "html-server"
version = "0.1.0"
edition = "2021"

[lib]
name = "html_server"
path = "src/lib.rs"

[[bin]]
name = "server"
path = "src/main.rs"

[dependencies]
zeroize = { version = "1.7", features = ["zeroize_derive"] }
secrecy = { version = "0.8", features = ["alloc", "serde"] }

